import React from 'react';
import styled from 'styled-components';
import * as skin from './skin';

const FooterWrapper = styled.div`${skin.FooterWrapper}`;

const Footer = props => (
  <FooterWrapper> aaa </FooterWrapper>
);


export default Footer;
